{
  "name": "portfolio",
  "private": true,
  "packageManager": "bun@1.1.34",
  "scripts": {
    "cf:whoami": "wrangler -c workers.toml whoami",
    "dev": "wrangler -c workers.toml dev",
    "deploy": "wrangler -c workers.toml deploy"
  },
  "devDependencies": {
    "wrangler": "^4.75.0"
  }
}
